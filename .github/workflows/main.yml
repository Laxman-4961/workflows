name: Import Postman Collection

on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }} 
    steps:
      - run: echo "$POSTMAN_API_KEY"
