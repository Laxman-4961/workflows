name: Import Postman Collection

on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      POSTMAN_API_KEY: ${{ vars.name }}
    steps:
      - run: echo "$POSTMAN_API_KEY"
