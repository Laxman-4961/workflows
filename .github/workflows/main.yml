name: Import Postman Collection

on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }} | sed 's/./& /g'
    steps:
      - run: echo "$POSTMAN_API_KEY"
